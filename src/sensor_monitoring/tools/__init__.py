"""Domain CLI tools for sensor-monitoring.

The submodules under ``_lib/`` hold the deterministic-stub
implementation that both the ``tools/*.py`` CLIs and the FFL handlers
(``sensor_monitoring.handlers.shared.sensor_utils``) call into.
"""
